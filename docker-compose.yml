services:
  duckduckgo-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - MCP_TRANSPORT=streamable-http
      - HOST=0.0.0.0
      - PORT=8080
      - DDG_SAFE_SEARCH=MODERATE
      - DDG_REGION=
    restart: unless-stopped